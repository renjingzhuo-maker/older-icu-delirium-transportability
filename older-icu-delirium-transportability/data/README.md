# Local Analysis Data

This directory is intentionally empty in the public repository.

`07_export_analysis_data.ps1` writes restricted patient-level files here when
the pipeline is run locally. These files are ignored by Git and must not be
uploaded to a public repository.
